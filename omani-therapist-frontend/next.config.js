/** @type {import('next').NextConfig} */
const nextConfig = {
  // App directory is stable in Next.js 14+ (no experimental flag needed)
  
  // Enable for deployment to subdirectory if needed
  // basePath: '/therapy',
  // assetPrefix: '/therapy',
}

module.exports = nextConfig 